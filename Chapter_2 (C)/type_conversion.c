// #include<stdio.h>

// int main(){
//     float a= 9.0;
//     float b= 5.0;
//     printf("The result is: %.2f\n" , (a/b));
//     return 0;
// }


// (int) & (int) operation would always be an integer.
// (int) & (float) operation would always be a float.
// (float) & (float) operation would always be a float.



#include<stdio.h>

int main(){
    int a= 6.7;
    printf("The value of a is: %d\n" , a);
    return 0;
}



// In the very above prpgram, demotion happens can, we have asked to return the value in integer and not as float.
// Also, the vice-versa would occur in case of integer to float.