#include<stdio.h>

int main(){
    char* st= "Harry";
    st= "Rohan";

    printf("%s" , st);
    return 0;
}



// By using pointers, we can change the existing string.



// When we are writing a string inside the curly({}) brackets, the always single quotes are used.
// Double quotes for writing a string is only used if string is written independently, without the curly({}) brackets. 