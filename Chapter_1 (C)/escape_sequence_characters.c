#include<stdio.h>
int main(){
    printf("Hello World!!!\n");
    printf("Hello \tWorld!!!");
    return 0;
}

// (Something with backslash is used as escape_sequence_characters).
// Here, (backslash-n) is used for generating a new line.
// Also, (backslash-t) is used to generate a tab space.