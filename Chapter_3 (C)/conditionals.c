#include<stdio.h>

int main(){
    int a= 6;
    if(a>18){
        printf("You are now an adult!!!");
    }
    else{
        printf("Error!!!");
    }
    return 0;
}



// (If-else), either of the two would be executed, i.e., the one whose condition gets satisfied, would be executed.